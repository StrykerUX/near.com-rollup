import { Site } from '@/components/Site';

/**
 * 2 · GUIDED — the hybrid. The script runs; the reader may take any control
 * the machine currently declares open, which pauses it and moves the playhead
 * to what they just did. Left alone, it picks the story back up.
 */
export default function Guided() {
  return <Site mode="guided" />;
}
